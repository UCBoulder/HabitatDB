import { EntityTarget } from '@typedorm/common';
import { Connection } from './connection';
import { EntityMetadata } from '../metadata/entity-metadata';
export declare class ConnectionMetadataBuilder {
    private connection;
    constructor(connection: Connection);
    buildEntityMetadatas(entities: EntityTarget<any>[] | string): EntityMetadata[];
    private loadEntitiesFromDirs;
    private recursiveLoadModulesFromFiles;
    private sanitizeDuplicates;
}
