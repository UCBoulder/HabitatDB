import { EntityTarget } from '@typedorm/common';
import { Batch } from './batch';
export interface ReadBatchItem<Entity, PrimaryKey> {
    item: EntityTarget<Entity>;
    primaryKey: PrimaryKey;
}
export declare class ReadBatch extends Batch<ReadBatchItem<any, any>> {
    add(batchItems: ReadBatchItem<any, any>[]): this;
    addGet<Entity, PrimaryKey = Partial<Entity>>(item: EntityTarget<Entity>, primaryKey: PrimaryKey): this;
}
