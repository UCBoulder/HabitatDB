import { DocumentClientTypes } from '@typedorm/document-client';
import { DynamoEntity, EntityTarget } from '@typedorm/common';
import { Connection } from '../connection/connection';
import { BaseTransformer, MetadataOptions } from './base-transformer';
/**
 * Note: To use any of the base transformer methods, this default entity transformer should be used
 */
export declare class EntityTransformer extends BaseTransformer {
    constructor(connection: Connection);
    /**
     * Converts dynamodb entity to model defined in entities
     * @param entityClass - Target class to look metadata off
     * @param dynamoEntity
     */
    fromDynamoEntity<Entity>(entityClass: EntityTarget<Entity>, dynamoEntity: DynamoEntity<Entity>, metadataOptions?: Pick<MetadataOptions, 'requestId'>): Entity;
    fromDynamoKeyToAttributes<Entity>(entityClass: EntityTarget<Entity>, dynamoKey: DocumentClientTypes.Key): Record<string, any>;
}
