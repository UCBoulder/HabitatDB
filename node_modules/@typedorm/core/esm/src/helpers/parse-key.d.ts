import { EntityAliasOrString } from '@typedorm/common';
export declare function parseKey<Entity>(key: EntityAliasOrString<any>, dict: Entity, { isSparseIndex }?: {
    isSparseIndex: boolean;
}): any;
