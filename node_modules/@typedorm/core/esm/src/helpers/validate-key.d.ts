import { EntityAliasOrString } from '@typedorm/common';
/**
 *
 * @param key key to validate
 * @param dict dictionary to validate key against, dictionary of attribute { name: type }
 */
export declare function validateKey(key: EntityAliasOrString<any>, dict: {
    [key: string]: string;
}, entityName?: string): void;
