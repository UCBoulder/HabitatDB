import { DocumentClientTypes } from '@typedorm/document-client';
export declare class TransactionCancelledException extends Error {
    name: string;
    code: string;
    cancellationReasons: {
        code?: string;
        message?: string;
        item?: DocumentClientTypes.AttributeMap;
    }[];
    constructor(code: string, cancellationReasons: TransactionCancelledException['cancellationReasons']);
}
