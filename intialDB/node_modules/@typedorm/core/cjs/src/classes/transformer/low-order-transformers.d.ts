import { Connection } from '../connection/connection';
import { DocumentClientRequestTransformer } from './document-client-request-transformer';
import { EntityTransformer } from './entity-transformer';
export interface LowOrderTransformers extends DocumentClientRequestTransformer, EntityTransformer {
}
export declare class LowOrderTransformers {
    connection: Connection;
    constructor(connection: Connection);
}
