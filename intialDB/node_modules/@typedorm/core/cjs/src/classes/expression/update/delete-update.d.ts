import { ScalarType, UpdateType } from '@typedorm/common';
import { Update } from './update';
export declare class DeleteUpdate extends Update {
    protected prefix: UpdateType.Action;
    /**
     * Deletes elements from a set
     */
    delete(key: string, value: ScalarType[]): this;
}
