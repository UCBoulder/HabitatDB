import { UpdateType } from '@typedorm/common';
import { Update } from './update';
export declare class RemoveUpdate extends Update {
    protected prefix: UpdateType.Action;
    remove(attrOrPath: string, options?: {
        atIndexes?: number[];
    }): this;
}
